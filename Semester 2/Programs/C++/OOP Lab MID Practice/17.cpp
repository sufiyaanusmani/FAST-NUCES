// Create a class called ADD that has two public static integer member variables named "a" and "b", and a public static member function named sum() that has no arguments but adds the two member variables together and returns their sum.
